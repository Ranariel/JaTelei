<UserControl x:Class="JaTelei.Client.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#0B1114"
             FontFamily="Segoe UI">

    <Grid Background="#0B1114">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.05*"/>
            <ColumnDefinition Width="430"/>
        </Grid.ColumnDefinitions>

        <Grid Margin="54">
            <StackPanel VerticalAlignment="Center" MaxWidth="520">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,26">
                    <Border Width="42" Height="42" BorderBrush="#24E0B6" BorderThickness="2" CornerRadius="8" Margin="0,0,14,0">
                        <Border Width="22" Height="22" Background="#1DBFCE" CornerRadius="4" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-6,-6"/>
                    </Border>
                    <TextBlock Text="JaTelei" Foreground="#F6FBFB" FontSize="30" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Text="Compartilhamento de tela simples." Foreground="#FFFFFF" FontSize="34" FontWeight="SemiBold" TextWrapping="Wrap"/>
                <TextBlock Text="Entre para iniciar uma sessao, escolher o destino e transmitir sua tela pela internet." Foreground="#9CABAF" FontSize="15" TextWrapping="Wrap" Margin="0,14,0,0"/>

                <UniformGrid Rows="1" Columns="3" Margin="0,32,0,0">
                    <Border Background="#121B1F" BorderBrush="#243136" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="&#xE7F4;" FontFamily="Segoe MDL2 Assets" Foreground="#28E4B0" FontSize="22"/>
                            <TextBlock Text="Tela" Foreground="#FFFFFF" FontSize="13" FontWeight="SemiBold" Margin="0,10,0,2"/>
                            <TextBlock Text="Escolha monitor ou janela." Foreground="#8FA0A4" FontSize="11" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#121B1F" BorderBrush="#243136" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="&#xE9D9;" FontFamily="Segoe MDL2 Assets" Foreground="#F5C542" FontSize="22"/>
                            <TextBlock Text="Conexao" Foreground="#FFFFFF" FontSize="13" FontWeight="SemiBold" Margin="0,10,0,2"/>
                            <TextBlock Text="Status claro da sessao." Foreground="#8FA0A4" FontSize="11" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#121B1F" BorderBrush="#243136" BorderThickness="1" CornerRadius="8" Padding="14">
                        <StackPanel>
                            <TextBlock Text="&#xE8C8;" FontFamily="Segoe MDL2 Assets" Foreground="#E8F8F5" FontSize="22"/>
                            <TextBlock Text="Convite" Foreground="#FFFFFF" FontSize="13" FontWeight="SemiBold" Margin="0,10,0,2"/>
                            <TextBlock Text="Envie o link ao visualizador." Foreground="#8FA0A4" FontSize="11" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>
            </StackPanel>
        </Grid>

        <Border Grid.Column="1" Background="#10191D" BorderBrush="#243136" BorderThickness="1,0,0,0">
            <Grid>
                <Border Width="318" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#121B1F" BorderBrush="#243136" BorderThickness="1" CornerRadius="8" Padding="26">
                    <StackPanel>
                        <TextBlock Text="Acessar JaTelei" Foreground="#FFFFFF" FontSize="22" FontWeight="SemiBold"/>
                        <TextBlock Text="Use seu apelido e senha para continuar." Foreground="#9CABAF" FontSize="12" Margin="0,6,0,22"/>

                        <TextBox x:Name="TxtUsername" Tag="Apelido" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                        <StackPanel Visibility="{Binding IsRegisterMode, Converter={StaticResource BoolToVis}}">
                            <TextBox Tag="Email opcional" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                            <TextBlock Text="Ajuda a recuperar sua conta depois." Foreground="#F6D36A" FontSize="11" Margin="0,0,0,10"/>
                        </StackPanel>

                        <Grid Height="44" Margin="0,0,0,8">
                            <PasswordBox x:Name="PwdBox"/>
                            <TextBlock x:Name="PwdPlaceholder" Text="Senha" Foreground="#6D7B80" FontSize="13" Margin="12,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" IsHitTestVisible="False"/>
                        </Grid>

                        <TextBlock Text="Sessao mantida neste computador" Foreground="#9CABAF" FontSize="12" Margin="0,4,0,4"/>

                        <TextBlock Text="{Binding ErrorMessage}" Foreground="#FF8A8A" FontSize="12" TextWrapping="Wrap" Margin="0,8,0,0" Visibility="{Binding ErrorMessage, Converter={StaticResource NotNullToVis}}"/>

                        <Button Margin="0,18,0,0" Padding="0,12" Style="{StaticResource BtnPrimary}" Command="{Binding SubmitCommand}" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBool}}">
                            <TextBlock Text="{Binding IsRegisterMode, Converter={StaticResource BoolToLoginRegister}}" FontSize="13"/>
                        </Button>

                        <Border Height="1" Background="#243136" Margin="0,18"/>

                        <Button Command="{Binding ToggleModeCommand}" Style="{StaticResource BtnGhost}" HorizontalAlignment="Center">
                            <TextBlock FontSize="12">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Nao tem conta? Criar agora"/>
                                        <Setter Property="Foreground" Value="#9CABAF"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsRegisterMode}" Value="True">
                                                <Setter Property="Text" Value="Ja tem conta? Entrar"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
